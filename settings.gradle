rootProject.name = 'build'

if (!System.getenv('PRINT_VERSION')) {
    include 'docker'
    include 'git:release'
    include 'java'
    include 'plugin:groovy'
    include 'plugin:release'
    include 'repository'
    include 'release'
    include 'zip'
}
